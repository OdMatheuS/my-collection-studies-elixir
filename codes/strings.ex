# following the introduction of strings in the Elixir School documentation ; link -> https://elixirschool.com/en/lessons/basics/strings#strings-0

# "Elixir strings are nothing but a sequence of bytes."
# understanding this concept I can create a text, using its reference in ASCII Code
best_brother = <<116, 97, 110, 106, 105, 114, 111, 32, 107, 97, 109, 97, 100, 111>>

# find the character code point using "?"
?t
# return -> 16

list_char_code_point = [?t, ?a, ?n, ?j, ?i, ?r, ?o]
# return -> 'tanjiro'
